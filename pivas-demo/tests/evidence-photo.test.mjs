import assert from'node:assert/strict';import{Evidence}from'../js/agent/evidence.js';assert.equal(Evidence.validateDraft({evidence:{primary:[{source:'leaflet.pdf'}],drugPhotos:[],conflicts:[]}}).canPublish,false);assert.deepEqual(Evidence.validateDraft({evidence:{primary:[{source:'leaflet.pdf'}],drugPhotos:[],conflicts:[]}}).blockers,['drug-photo-required']);assert.equal(Evidence.validateDraft({evidence:{primary:[{source:'leaflet.pdf'}],drugPhotos:[{source:'drug.jpg'}],conflicts:[]}}).canPublish,true);console.log('evidence photo tests passed');
