import assert from'node:assert/strict';import{readFileSync,readdirSync,statSync}from'node:fs';import{join,resolve}from'node:path';const root=resolve(new URL('..',import.meta.url).pathname),html=readFileSync(join(root,'index.html'),'utf8');assert.match(html,/PIVAS 智能用药助手 3\.0/);assert.match(html,/type="module"[^>]+js\/app\.js/);for(const legacy of['v05','v06','v08','v09','v10','v20','v21','v22','v23','v24'])assert.ok(!html.includes(legacy),`legacy runtime ref ${legacy}`);const files=[];function walk(d){for(const n of readdirSync(d)){const p=join(d,n);if(statSync(p).isDirectory())walk(p);else if(p.endsWith('.js'))files.push(p)}}walk(join(root,'js'));assert.ok(files.length>=10,'modular v3 js files');console.log('static tests passed');
