import assert from'node:assert/strict';import{QAAgent}from'../js/agent/qa-agent.js';const a=new QAAgent();assert.equal(a.askLocal('怎么配？'),'请先选择药品');a.setDrug({prep:['复溶10 mL'],storage:['2-8℃'],infusion:['30 min'],warnings:['不得振摇']});assert.match(a.askLocal('怎么配？'),/复溶10 mL/);assert.match(a.askLocal('保存多久？'),/2-8℃/);assert.match(a.askLocal('输注要求？'),/30 min/);assert.match(a.askLocal('注意事项？'),/不得振摇/);console.log('agent tests passed');
