import assert from'node:assert/strict';import fs from'node:fs';const ui=fs.readFileSync(new URL('../js/ui/ai-builder.js',import.meta.url),'utf8'),gen=fs.readFileSync(new URL('../js/agent/dual-card-generator.js',import.meta.url),'utf8');assert.match(ui,/materialCaptureInput/);assert.match(ui,/drugCaptureInput/);assert.match(ui,/至少1张/);assert.match(ui,/AI自动分析并生成知识卡/);assert.match(gen,/支持图片输入/);assert.match(gen,/image_url/);assert.match(gen,/evidence\.conflicts/);console.log('dual builder static tests passed');
