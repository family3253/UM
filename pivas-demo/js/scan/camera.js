import{Store}from'../core/store.js';export class Camera{constructor(){this.stream=null;this.video=null;this.facingMode=Store.get('camera','environment')}async start(video,facingMode=this.facingMode){this.stop();this.video=video;this.facingMode=facingMode;this.stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:facingMode},width:{ideal:1920},height:{ideal:1080}},audio:false});video.srcObject=this.stream;await video.play();Store.set('camera',facingMode);return facingMode}async switch(){this.facingMode=this.facingMode==='environment'?'user':'environment';if(this.video)await this.start(this.video,this.facingMode);return this.facingMode}stop(){if(this.stream)this.stream.getTracks().forEach(t=>t.stop());this.stream=null;if(this.video)this.video.srcObject=null}}
