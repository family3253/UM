let worker=null;export const OCR={async recognize(canvas,onProgress=()=>{}){if(typeof TextDetector!=='undefined'){const blocks=await new TextDetector().detect(await createImageBitmap(canvas));return blocks.map(x=>x.rawValue).join('\n')}if(!globalThis.Tesseract)throw new Error('Tesseract.js 未加载');if(!worker)worker=await Tesseract.createWorker('chi_sim+eng',1,{logger:m=>m.status==='recognizing text'&&onProgress(m.progress||0)});const ret=await worker.recognize(canvas);return ret.data.text||''},async terminate(){if(worker){await worker.terminate();worker=null}}};
