import{DrugResolver}from'../drugs/resolver.js';
function makeRecognition({onText=()=>{},onFinal=()=>{},onError=()=>{}}={}){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){onError(new Error('当前浏览器不支持 Web Speech API'));return null}const r=new SR();r.lang='zh-CN';r.continuous=false;r.interimResults=true;let last='';r.onresult=e=>{let text='';let hasFinal=false;for(let i=e.resultIndex;i<e.results.length;i++){text+=e.results[i][0].transcript;if(e.results[i].isFinal)hasFinal=true}last=text.trim();onText(last);if(hasFinal&&last)onFinal(last)};r.onerror=e=>onError(new Error(e.error||'语音识别失败'));r.onend=()=>{if(last)onText(last)};r.start();return r}
export const VoiceSearch={supported:typeof window!=='undefined'&&!!(window.SpeechRecognition||window.webkitSpeechRecognition),transcribe(options={}){return makeRecognition(options)},start({onText=()=>{},onCandidates=()=>{},onError=()=>{}}={}){return makeRecognition({onText:t=>{onText(t);onCandidates(DrugResolver.resolve(t).slice(0,4))},onError})}};
