import{DrugResolver}from'../drugs/resolver.js';export const VoiceSearch={supported:typeof window!=='undefined'&&!!(window.SpeechRecognition||window.webkitSpeechRecognition),start({onText=()=>{},onCandidates=()=>{},onError=()=>{}}={}){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){onError(new Error('当前浏览器不支持 Web Speech API'));return null}const r=new SR();r.lang='zh-CN';r.continuous=false;r.interimResults=true;r.onresult=e=>{let text='';for(let i=e.resultIndex;i<e.results.length;i++)text+=e.results[i][0].transcript;text=text.trim();onText(text);onCandidates(DrugResolver.resolve(text).slice(0,4))};r.onerror=e=>onError(new Error(e.error||'语音识别失败'));r.start();return r}};
