import{Evidence}from'../agent/evidence.js';const now=()=>new Date().toISOString();export class KnowledgeService{constructor(storage=globalThis.localStorage){this.storage=storage;this.draftsKey='pivas3:drafts';this.pubKey='pivas3:published'}read(k){try{return JSON.parse(this.storage?.getItem(k)||'[]')}catch{return[]}}write(k,v){this.storage?.setItem(k,JSON.stringify(v));return v}createDraft(payload){const drafts=this.read(this.draftsKey),stamp=Date.now(),item={...payload,id:payload.id||`draft_${stamp}_${Math.random().toString(36).slice(2,7)}`,drugId:payload.drugId||`USER_${stamp}`,status:'draft',createdAt:now()};drafts.unshift(item);this.write(this.draftsKey,drafts);return item}review(id,{approved,reviewer}){const drafts=this.read(this.draftsKey),item=drafts.find(x=>x.id===id);if(!item)throw new Error('draft not found');item.status=approved?'approved':'rejected';item.reviewer=reviewer||'pharmacist';item.reviewedAt=now();this.write(this.draftsKey,drafts);return item}publish(id){const drafts=this.read(this.draftsKey),item=drafts.find(x=>x.id===id);if(!item)throw new Error('draft not found');if(item.status!=='approved')throw new Error('draft must be approved before publish');const check=Evidence.validateDraft(item);if(!check.canPublish)throw new Error(check.blockers.some(x=>x.startsWith('conflict'))?'conflict blocks publish':check.blockers.includes('drug-photo-required')?'drug photo required':'primary evidence required');const published=this.read(this.pubKey),prev=published.filter(x=>x.drugId===item.drugId||x.name===item.name),out={...item,status:'published',version:prev.length+1,publishedAt:now()};published.unshift(out);this.write(this.pubKey,published);return out}listDrafts(){return this.read(this.draftsKey)}listPublished(){return this.read(this.pubKey)}}
